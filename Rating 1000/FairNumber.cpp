#include <bits/stdc++.h>
using namespace std;

void solve(int ind, string s, ) void fun()
{
    long long n;
    cin >> n;
    string s = to_String(n);
    string ans = s;
    solve(0, s, ans);
    cout << ans << endl;
}

int main()
{
    long t;
    cin >> t;
    while (t--)
    {
        fun();
    }
}